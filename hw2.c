#include <stdio.h>

int main(void) {
  int x,y;
  printf("enter the values for x,y\n");
  scanf("%d%d",&x,&y);
  if (x==y)
  printf("x and y are equal\n");
  else
  printf("x and y are not equal\n");
  return 0;
}
